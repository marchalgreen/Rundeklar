{
  "name": "herlev-hjorten",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter webapp dev",
    "build": "pnpm --filter common build && pnpm --filter webapp build",
    "lint": "pnpm --filter webapp lint",
    "test": "pnpm --filter webapp test"
  },
  "devDependencies": {},
  "pnpm": {
    "packageManager": "pnpm@9.6.0"
  }
}
