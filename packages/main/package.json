{
  "name": "@badminton/main",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/main.cjs",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --format=cjs --platform=node --target=node20 --external:electron --external:better-sqlite3 --outfile=dist/main.cjs --sourcemap",
    "test": "vitest run"
  },
  "dependencies": {
    "better-sqlite3": "^10.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.8",
    "esbuild": "^0.23.0",
    "vitest": "^2.0.5"
  }
}
