// src/components/calendar/CalendarMonth.tsx
'use client';

export default function CalendarMonth() {
  return (
    <div className="grid h-full place-items-center text-muted">
      Month view (compact) — coming next.
    </div>
  );
}
